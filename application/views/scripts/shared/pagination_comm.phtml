<?if($this->pageCount>1):?>
	<div class="pager_holder gray_pager_holder">
		<div class="pagination">			
			<? if (isset($this->previous)) :?>
				<a id="prev" href="#" class="previouslink"></a>
			<? endif ?>	
				<a href="#"><?= $this->current ?></a>
			<? if (isset($this->next)) :?>
				<a id="next" href="#" class="nextlink"></a>
			<? endif ?>	
		</div>
	</div>
	<script type="text/javascript">
		<? 
			$url = preg_replace("/?cp=\d{1,3}/", "", $_SERVER['REQUEST_URI']) . "?cp=";
		?>
	
		var link_prev = <? echo "'" . $url . $this->previous . "';" ?>
		var link_next = <? echo "'" . $url . $this->next . "';" ?>
		$('#prev').attr('href', link_prev);
		$('#next').attr('href', link_next);
	</script>
<?endif?>